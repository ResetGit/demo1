server:
  port: 8080


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/diancan?serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml
  type-aliases-package: com.example.demo.*.pojo


#showSql
logging:
  level:
    com.demo: debug
    spring: info